apiVersion: v1
kind: Service
metadata:
  name: auth-service-db
  labels:
    app: auth-service-db
spec:
  type: ClusterIP
  selector:
    app: auth-service-db
  ports:
    - protocol: "TCP"
      port: 3000
      targetPort: 3000
