apiVersion: v1
kind: Service
metadata:
  name: gateway
  labels:
    app: gateway
spec:
  type: LoadBalancer
  selector:
      app: gateway
  ports:
    - protocol: "TCP"
      port: 3000
      targetPort: 3000
