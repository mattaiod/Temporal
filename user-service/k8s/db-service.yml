apiVersion: v1
kind: Service
metadata:
  name: user-service-db
  labels:
    app: user-service-db
spec:
  type: ClusterIP
  selector:
    app: user-service-db
  ports:
    - protocol: "TCP"
      port: 3000
      targetPort: 3000
